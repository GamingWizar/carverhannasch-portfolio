export type SkillCardProps = {
  title: string;
  skillList: string[];
};

export type ProjectCardProps = {
  title: string;
  img: any;
  description: string;
  liveLink?: string;
  gitHubLink?: string;
};
